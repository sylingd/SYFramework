{
	"name": "Controller",
	"namespace": "sy\\base",
	"description": "此类不可直接实例化，必须继承为Controller",
	"var": {
		"_framework_m": "已经加载的Model列表，请勿直接操作"
	},
	"method": {
		"loadModel": {
			"name": "加载Model",
			"description": "将Model加载至Controller内，方便调用",
			"access": "protected",
			"param": {
				"modelName": {
					"type": "string",
					"description": "Model名称"
				},
				"loadAs": {
					"type": "string",
					"description": "加载为，例如<code>$this->loadModel('myModel', 'm')</code>则在Controller内可直接通过<code>$this->m</code>调用"
				}
			}
		}
	}
}